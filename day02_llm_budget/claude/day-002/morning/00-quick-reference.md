# 快速参考卡片（手写前先看这里）

> 08:00 关闭 AI 工具，把这 3 张表手写到纸上。
> 手写完毕后，合上电脑，对照 05-interview-attacks.md 自问自答 3 道题。

---

## 表1：Token消耗分布（手写版）

```
┌──────────────────────────────────────────────────────────────┐
│              一次 TMS Agent LLM 调用 Token 分布              │
├────────────────────┬─────────┬──────────────────────────────┤
│ 组成部分            │ Tokens  │ 是否容易被忽略               │
├────────────────────┼─────────┼──────────────────────────────┤
│ 对话历史 History   │   800   │ 🔥 极高 - 多轮后指数膨胀     │
│ Tools Schema × 5   │   600   │ 🔥 极高 - 开发者最常忽略    │
│ System Prompt      │   500   │ ⚠️  中 - 写太长会失控        │
│ Completion 输出     │   300   │ ✅ 容易感知                  │
│ User Query         │   200   │ ✅ 容易感知                  │
├────────────────────┼─────────┼──────────────────────────────┤
│ 合计               │  2400   │ 输入1900 / 输出300           │
├────────────────────┴─────────┴──────────────────────────────┤
│ 成本（DeepSeek V3）：                                        │
│   输入：1900/1000 × ¥0.001 = ¥0.0019                        │
│   输出： 300/1000 × ¥0.004 = ¥0.0012                        │
│   合计：¥0.0031 ≈ 0.031 分/次                               │
│                                                              │
│ ⚠️  隐藏杀手：8个工具 × 150tokens ≈ 1200 hidden tokens/次   │
└──────────────────────────────────────────────────────────────┘
```

---

## 表2：Retry 分类决策表（手写版）

```
┌──────────────────────┬────────┬──────────────────┬──────┬──────────────────────┐
│ 错误码 / 场景         │ 重试?  │ 重试策略          │ 最多 │ 理由                 │
├──────────────────────┼────────┼──────────────────┼──────┼──────────────────────┤
│ 429 Rate Limit       │ ✅ 是  │ 指数退避+Jitter  │ 3次  │ 限流是临时的         │
│ 500 Internal Error   │ ✅ 是  │ 线性退避(2s,4s)  │ 2次  │ 偶发故障             │
│ 502 Bad Gateway      │ ✅ 是  │ 线性退避(2s,4s)  │ 2次  │ 网关临时问题         │
│ 503 Unavailable      │ ✅ 是  │ 指数退避+Jitter  │ 3次  │ 服务降级，可恢复     │
│ 超时 (>30s)          │ ✅ 是  │ 立即重试          │ 1次  │ 网络抖动             │
├──────────────────────┼────────┼──────────────────┼──────┼──────────────────────┤
│ 400 Bad Request      │ ❌ 否  │ —                │ 0次  │ 参数错误，重试必败   │
│ 401 Unauthorized     │ ❌ 否  │ —                │ 0次  │ Key无效，重试无意义  │
│ 403 Forbidden        │ ❌ 否  │ —                │ 0次  │ 权限问题             │
│ length (截断)        │ ❌ 否  │ → Fallback规则   │ 0次  │ 上下文溢出，必再截断 │
│ content_filter       │ ❌ 否  │ → 安全拒绝提示   │ 0次  │ 安全拦截，必再拦截   │
└──────────────────────┴────────┴──────────────────┴──────┴──────────────────────┘

关键记忆：4xx = 客户端错误 = 不可重试
           5xx = 服务端错误 = 可重试（但有上限）
           length = 逻辑错误 = 不可重试
```

---

## 表3：Temperature 决策矩阵（手写版）

```
┌──────────────────────────┬─────────────┬───────┬──────────────────────────────┐
│ 场景                      │ Temperature │ Top_p │ 理由                         │
├──────────────────────────┼─────────────┼───────┼──────────────────────────────┤
│ TMS 设备故障诊断          │ 0.0 - 0.2   │ 0.1   │ 诊断必须确定，拒绝幻觉       │
│ TMS OTA 指令生成          │ 0.0         │ 0.1   │ 指令是命令式的，零容忍随机   │
│ TMS 脚本推送验证          │ 0.0         │ 0.1   │ 语法不容创意，随机破坏结构   │
├──────────────────────────┼─────────────┼───────┼──────────────────────────────┤
│ 养老用药咨询              │ 0.0         │ 0.1   │ 医疗绝对严谨，幻觉=事故      │
│ 养老健康风险评估          │ 0.1         │ 0.2   │ 风险等级需确定，措辞可微变   │
├──────────────────────────┼─────────────┼───────┼──────────────────────────────┤
│ OTT 卡顿根因分析          │ 0.2 - 0.3   │ 0.5   │ 分析框架稳，措辞可多样       │
│ OTT 运营日报生成          │ 0.5 - 0.7   │ 0.9   │ 报告可有文采，数字必须准确   │
├──────────────────────────┼─────────────┼───────┼──────────────────────────────┤
│ 多Agent规划（Planner）    │ 0.2         │ 0.5   │ 规划稳健，但需适应不同场景   │
└──────────────────────────┴─────────────┴───────┴──────────────────────────────┘

核心原则：安全场景 Temperature 是架构决策，不是用户偏好，系统层锁定
```

---

## 今日核心技术判断（背诵）

> 在生产级 Agent 系统中，LLM响应的 `finish_reason` 不能简单信任默认的 `stop`，
> 因为 `length` 截断会导致 Agent 拿到残缺 JSON 并触发不可预期的工具调用。
>
> **我的设计选择**：在 LLMResponse 解析层强制校验 `finish_reason`，
> 非 `stop` 立即抛 `TruncationError` 并触发 Fallback 到规则引擎，
> 同时将该请求标记为**不可重试**。
>
> **代价**：损失了部分长文本任务的自动处理能力。
> **收益**：彻底杜绝了因截断导致的 Agent 运行时崩溃和安全事故。

---

## 学习路径（08:00-11:00）

```
08:00 - 08:30  手写 3 张表（纸质，关闭电脑）
08:30 - 09:30  精读 01-token-economics.md（Token经济学 + 隐藏开销）
09:30 - 10:00  精读 02-context-window.md（KV Cache + Lost in the Middle）
10:00 - 10:30  精读 03-temperature-params.md（参数原理 + 路由矩阵）
10:30 - 11:00  精读 04-finish-reason.md + 05-interview-attacks.md
               自测：3道面试题脱稿回答
               目标：30秒内说清"finish_reason=length为什么不可重试"
```
